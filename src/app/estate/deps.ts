/** Sibling-module imports for the Estate screen, gathered so tests can mock one path. */
export { buyFurnishing, estateProgress, loadProfile } from '../../engine/progress'
export { rankFor, rankName } from '../../engine/ranks'
