/** Sibling-module imports for the Rank-up screen, gathered so tests can mock one path. */
export { collection, loadProfile } from '../../engine/progress'
export { RANKS, rankFor, rankName } from '../../engine/ranks'
