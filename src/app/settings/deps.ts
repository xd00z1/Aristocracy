/** Sibling-module imports for the Settings screen, gathered so tests can mock one path. */
export { collection, loadProfile, resetAll, saveProfile } from '../../engine/progress'
export { RANKS, rankFor, rankName } from '../../engine/ranks'
